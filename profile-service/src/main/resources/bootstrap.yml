
spring:
  application:
    name: Profile

server:
  port: 0
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true

  instance:
    instance-id: ${spring.application.name}-${server.port}


#spring:
#  cloud:
#    config:
#      uri: http://localhost:8191
