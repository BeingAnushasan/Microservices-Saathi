server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BeingAnushasan/microservice-config-store
          search-paths:
            - 'service-config/*service'
